# requests

request get_status = 0 {
}

request set_interval = 1 {
    interval: pod(u64),
}

# events

event interval = 0 {
    interval: pod(u64),
}

event inhibitor = 1 {
    surface: id(wl_surface),
    client_id: pod(u64),
    pid: pod(u64),
    comm: str,
}
