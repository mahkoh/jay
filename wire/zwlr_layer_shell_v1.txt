singleton

# requests

request get_layer_surface {
    id: id(zwlr_layer_surface_v1) (new),
    surface: id(wl_surface),
    output: id(wl_output) (nullable),
    layer: u32,
    namespace: str,
}

request destroy (destructor, since = 3) {
}
