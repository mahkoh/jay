event_handler

# requests

request copy {
    buffer: id(wl_buffer),
}

request destroy (destructor) {
}

request copy_with_damage (since = 2) {
    buffer: id(wl_buffer),
}

# events

event buffer {
    format: u32,
    width: u32,
    height: u32,
    stride: u32,
}

event flags {
    flags: u32,
}

event ready {
    tv_sec: u64,
    tv_nsec: u32,
}

event failed {

}

event damage (since = 2) {
    x: u32,
    y: u32,
    width: u32,
    height: u32,
}

event linux_dmabuf (since = 3) {
    format: u32,
    width: u32,
    height: u32,
}

event buffer_done (since = 3) {
}
