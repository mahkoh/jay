# requests

request accept {
    serial: u32,
    mime_type: optstr,
}

request receive {
    mime_type: str,
    fd: fd,
}

request destroy {
}

request finish {
}

request set_actions {
    dnd_actions: u32,
    preferred_action: u32,
}

# events

event offer {
    mime_type: str,
}

event source_actions {
    source_actions: u32,
}

event action {
    dnd_action: u32,
}
