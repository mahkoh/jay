# requests

request sync = 0 {
    callback: id(wl_callback),
}

request get_registry = 1 {
    registry: id(wl_registry),
}

# events

event error = 0 {
    object_id: id(object),
    code: u32,
    message: str,
}

event delete_id = 1 {
    id: u32,
}
