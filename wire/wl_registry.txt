# requests

request bind = 0 {
    name: u32,
    interface: str,
    version: u32,
    id: id(object),
}

# events

event global = 0 {
    name: u32,
    interface: str,
    version: u32,
}

event global_remove = 1 {
    name: u32,
}
