request set_selection {
    source: id(zwp_primary_selection_source_v1) (nullable),
    serial: u32,
}

event data_offer {
    offer: id(zwp_primary_selection_offer_v1) (new),
}

event selection {
    id: id(zwp_primary_selection_offer_v1) (nullable),
}

request destroy (destructor) {
}
