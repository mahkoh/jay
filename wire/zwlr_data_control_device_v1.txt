# requests

request set_selection {
    source: id(zwlr_data_control_source_v1) (nullable),
}

request destroy (destructor) {

}

request set_primary_selection (since = 2) {
    source: id(zwlr_data_control_source_v1) (nullable),
}

# events

event data_offer {
    id: id(zwlr_data_control_offer_v1) (new),
}

event selection {
    id: id(zwlr_data_control_offer_v1) (nullable),
}

event finished (dead) {
}

event primary_selection (since = 2) {
    id: id(zwlr_data_control_offer_v1) (nullable),
}
